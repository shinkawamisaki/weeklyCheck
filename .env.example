# (Required) The full, raw URL to your checkRisk.sh script on GitHub.
# This should be a link to a specific commit or tag to ensure consistent execution.
SCRIPT_SOURCE_URL="https://raw.githubusercontent.com/<owner>/<repo>/<commit-or-tag>/checkRisk.sh"

# (Optional) Set to "true" or "1" to enable OpenAI summary.
# If enabled, you must also configure the 'openai/prod/key' secret in AWS Secrets Manager.
POLISH_WITH_OPENAI="false"
